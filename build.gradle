apply plugin: 'java'
apply plugin: 'groovy'
apply plugin: 'idea'

repositories {
    mavenCentral()
}

// Adds capability to run on command line via `./gradlew run`
// Adds capability to build m2sm.jar & m2sm.exe
apply from: 'build-executable.gradle'